# README

Export the API key:
```bash
export NOMICS_KEY="XXXXXXX"
```

Run the website, the server will start at localhost:3000:
```bash
bundle exec rails s
```

Run tests:
```bash
bundle exec rspec
```